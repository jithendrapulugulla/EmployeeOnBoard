# Azure Production Environment Variables
NODE_ENV=production

# MongoDB Atlas Connection String (set in Azure App Service Environment Variables)
MONGODB_URI=${MONGODB_URI}

# Port - Azure App Service will set PORT environment variable
PORT=8080

# JWT Secret (set in Azure Key Vault or App Service config)
JWT_SECRET=${JWT_SECRET}

# Email Configuration (Gmail, SendGrid, or Azure Communication Services)
EMAIL_USER=${EMAIL_USER}
EMAIL_PASSWORD=${EMAIL_PASSWORD}
EMAIL_SERVICE=gmail

# Frontend URL for CORS
FRONTEND_URL=https://your-app-name.azurewebsites.net

# Application Settings
NODE_ENV=production
LOG_LEVEL=info
